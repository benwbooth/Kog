# Cog UI parity matrix

Reference: `losnoco/Cog` commit
`c17be85654a64170c86bb8bbb4b59fd7b6795722` (2026-08-16), especially
`Base.lproj/MainMenu.xib`, the other `Base.lproj/*.xib` windows, the Swift
preferences panes, and `.github/images/MainWindow.png`.

`Done` requires matching states, interactions, keyboard behavior, resizing,
and deterministic visual captures on all three desktop platforms. `Partial`
means the control is real and usable but does not yet cover Cog's complete
surface.

## Windows and panels

| State | Cog surface | Current Kog coverage | Required parity work |
| --- | --- | --- | --- |
| Partial | Main window and toolbar | Cog-shaped toolbar, themed Previous/Play-Pause/Stop/Next controls, position, volume, search, split content, duration footer, and the user-selected cross-platform hamburger command menu render and operate without overriding the active Qt/KDE palette. The file-tree toggle sits beside the hamburger on the left. The unified chrome uses macOS traffic lights only on macOS; other platforms place native-themed minimize, maximize/restore, and close controls on the right. Shuffle cycles through Cog's Off/Albums/All policy and Repeat through Off/One/Album/All with compact mode badges and exact-mode tooltips. | Toolbar customization, speed/pitch controls, and reference-image diffs |
| Partial | File tree | Real Qt `TreeView` over a lazy, filesystem-watching `QFileSystemModel`, with single-click directory expansion, themed icons, parent/root selection, tooltips, file/folder double-click using Cog's opening policy, pointer-tracked file/folder drag into the playlist, and standards-based external URI drops | Smart folders, expansion persistence, context actions, multi-selection actions, and corpus-driven comparison of Cog's exact drag gestures |
| Partial | Playlist table | Metadata rows, alternating backgrounds, Ctrl/Command and Shift multi-selection, keyboard range selection, double-click playback, a themed play/pause state glyph beside the live audio-reactive waveform, filtering, batch removal, external/tree drop-add, internal multi-row drag reordering with a themed insertion marker, a playlist context menu, decoder-provided subsong expansion, stable type-aware header sorting with original-order restoration, and Cog's complete 19-column surface. Header resizing, separator double-click auto-fit, visibility, and move-left/right order persist as one validated layout with migration from Kog's earlier nine-width setting. Cog-style Save As and Save Selection As write and reopen M3U/M3U8/PLS while preserving local, remote, CUE, subsong, and archived-member identities. Context and playback menus expose Cog's per-entry queue, Clear Queue, and Stop After Selection actions; the status column prioritizes Stop After, current playback, then queued state with queue-position tooltips, and state is remapped safely across row moves/removals. | Ratings, play counts, inline editing, queue/session persistence across launches, and corpus-driven comparison of Cog's exact drag gestures |
| Partial | Info Inspector | Real detached window follows the playing track and displays common/technical metadata including length and filename | Album artist, composer, full date semantics, true codec identification, extracted artwork, selection-vs-playing policy, and saved placement |
| Partial | Mini player | Detached compact transport with title, artist, and seek | Cog's mini/dual mode switching, sizing/chrome, volume and remaining controls, placement persistence, and capture corpus |
| Partial | Preferences | Cog-derived General, Playback, Output, Synthesis, and Updating sections use themed Qt controls. Playlist opening behavior preserves Cog's clear-and-play, enqueue, and enqueue-and-play choices; synthesis persists RustySynth/SF2, OPL3Windows, Nuked SC-55, or Munt MT-32/CM-32L, defaults Munt's GM program compatibility map on, and uses native pickers for SF2 and user ROM locations. General settings persist optional track-change notifications with Previous, Pause, Stop, and Next actions on notification servers that support them. Output enumerates native backend devices, keeps System Default first, persists ID and name with name-based recovery, refreshes live, and switches active playback while preserving position and pause state. A searchable Formats page exposes every extension advertised by the active decoder registry, including native AdPlug and vgmstream runtime tables plus playlists and in-process archives, so the UI stays synchronized with the bundled build. | Modifier-specific opening behavior, remaining synthesis/effect controls, appearance, automatic device hot-plug notifications, output-buffer/sample-format controls, shortcuts, Last.fm, remote-control, time-stretch, and platform path panes |
| Partial | Equalizer | The hamburger View menu and `Ctrl+E` toggle a themed Cog-sized window with enable and genre-tracking controls, Cog's exact 22-preset library and 10-to-31-band interpolation, preamp, all 31 bands, Flatten EQ, Level Preamp, tooltips, and Cog's right-drag curve editing gesture. Strict settings persist across launches, genre changes select the longest matching preset, and one shared Q=1.4 peaking-biquad DSP path applies live to every decoder with independent channel state and resets on track changes and seeks. | Direct PCM comparison with Cog's vDSP implementation, saved window placement, accessibility audit, and deterministic light/dark captures on Linux, Windows, and macOS |
| Partial | Lyrics | Cog-sized resizable read-only scrolling window follows the playing track, with ID3v2 USLT and generic tag extraction in Cog's fallback order; the hamburger View submenu exposes it with `Ctrl+Shift+L` | Decoder-native lyrics outside Lofty's tag coverage, selection-vs-playing policy, synchronized lyrics, placement persistence, and deterministic populated/empty captures |
| Not started | Spectrum and SC-55 visualization | None | Spectrum modes/settings and the Nuked SC-55 visualization window |
| Not started | Spotlight/library search | None | Search location, result columns/sorting, add-to-playlist, reveal action, and keyboard flow |
| Partial | Tag editor | The hamburger menu, playlist context menu, and `Ctrl+Shift+E` open a detached system-themed multi-track editor. It shows mixed-value states, writes only changed common fields, supports comments and lyrics, previews/replaces/removes the displayed embedded cover through a platform-native image picker, rejects remote/archive/subsong sources, and reloads edited playlist entries. Editing the active file stops the decoder first and restores its exact position and paused/playing state after reload. Real-file tests cover mixed and partial multi-file edits, removal, artwork round trips, malformed input, and size/selection limits. Cog's checked-in `TagEditorController` is disabled, so this surface follows Cog's active TagLib writer semantics inside the established CogX-style shell rather than claiming a finished original window to clone. | Cog's broader arbitrary TagLib property-map and multi-value surface, multiple-artwork management, batch track sequencing, ratings/play counts, undo or atomic multi-file rollback, broad format corpora, saved placement, and light/dark runtime captures on Linux, Windows, and macOS |
| Partial | Open URL and path suggester | The hamburger menu exposes Cog's Add URL command with its `Ctrl+Shift+O` shortcut, themed modal HTTP(S) entry, Rust URL validation, Cog opening-policy integration, and linked-FFmpeg HTTP/HLS playback | URL history/autocomplete, richer inline validation and loading/error states, local sandbox/path suggestion equivalents, and deterministic captures |
| Not started | About, credits, and feedback | None | Cross-platform equivalents using Kog identity and third-party notices |

## Interaction and integration surface

| State | Behavior |
| --- | --- |
| Partial | Platform-native file/folder/save dialogs, local drag-and-drop, search, core transport shortcuts, seek, volume, end-of-track advance |
| Partial | Kog deliberately uses one toolbar hamburger menu instead of Cog's macOS menu bar. Implemented file/URL and playlist opening, Save As, Save Selection As, tag editing, playlist-removal, View-window (including Equalizer), playback, Cog's explicit queue and Stop After Selection, all repeat/shuffle modes, preferences, and quit commands retain icons, shortcuts, and state-based enablement; Open Recent and previous/next album remain |
| Partial | Linux MPRIS2 publishes live playback state, metadata, position, seeking, volume, repeat/shuffle, and transport capabilities under `org.mpris.MediaPlayer2.kog`. Plasma media controls and hardware media keys can call Raise, Previous, Play, Pause, PlayPause, Stop, Next, Seek, SetPosition, OpenUri, volume, repeat, and shuffle without touching Qt or the audio engine from the D-Bus worker thread. Native track notifications and tray controls remain available independently. Remaining work includes Windows SMTC, macOS MediaRemote/Now Playing, MPRIS TrackList, artwork URLs, notification artwork, Last.fm, remote control, accessibility, and localization. |

## Visual verification states

Each finished surface needs fixed-size captures for empty, populated, selected,
playing, paused, stopped, filtered, loading, and error states where applicable.
The test harness must use deterministic metadata and fonts, compare structure
and pixels with an explicit tolerance, and retain Cog and Kog images together
so visual similarity cannot be asserted from memory.
