# Cog format parity matrix

Reference: `losnoco/Cog` commit
`c17be85654a64170c86bb8bbb4b59fd7b6795722` (2026-08-16). This inventory is
derived from Cog's decoder and container plugins. `Done` means a backend and
its parity corpus pass the fidelity gates in `ARCHITECTURE.md`; `Partial` means
real code exists but the full family has not passed those gates; `Not started`
is required work, not a claim of support.

## Conventional audio

| State | Cog family | Extensions / source behavior | Kog backend |
| --- | --- | --- | --- |
| Partial | CoreAudio/FFmpeg overlap | AAC/ADTS, AIFF, ALAC, CAF, FLAC, MP1/2/3, MP4/M4A, Ogg/Vorbis, Opus, WAV, Matroska/WebM combinations accepted by Symphonia. WAV and FLAC fixtures have passed the current probe/play/advance smoke; the family corpus is incomplete. | `rodio-symphonia` |
| Partial | FFmpeg | Cog's WMA, ASF, TAK, M4R, M2A/MPA, APE, AC-3, DTS/DTS-HD, TTA, TwinVQ, RealAudio, WebA, DSF/DFF/DSDIFF/WSd, and overlapping conventional extensions route through a real libavformat/libavcodec/libswresample backend. It keeps native rate/channel layout, converts to interleaved float PCM, reads common metadata/properties, and seeks through the demuxer. The pinned Nix shell supplies FFmpeg 9.0.1 with GPL and version-3 components disabled and an LGPL-2.1-or-later license result. A deterministic four-frame AC-3 fixture passes routing, duration, bitrate, codec, audible PCM, seek, priority, and EOS tests. The wider format/metadata corpus, exact seek across every demuxer, DSD policy, gapless trim, chapters/subtracks, artwork, content probing, remote custom I/O, and Windows/macOS build gates remain. | system FFmpeg via pkg-config; pinned Nix configuration tested at 9.0.1 |
| Partial | WavPack | WV and WVP now route through FFmpeg. A WavPack/correction-file corpus, explicit sibling WVC resolution, lossless/float/DSD properties, and comparison with Cog's dedicated libwavpack path remain. | FFmpeg baseline; dedicated libwavpack only if parity requires it |
| Partial | Musepack | MPC now routes through FFmpeg; SV7/SV8 corpus, seek, metadata, and behavioral comparison with Cog's libmpcdec plugin remain. | FFmpeg baseline; libmpcdec fallback if required |
| Partial | Shorten | SHN now routes through FFmpeg; a redistributable corpus and comparison with Cog's dedicated Shorten decoder remain. | FFmpeg baseline; dedicated fallback if required |
| Partial | APL | Monkey's Audio Image Link files parse Cog's case-insensitive header and fields with original CRLF or portable LF endings, normalize relative Windows paths, resolve a local image, and enforce the declared start/finish PCM-frame range through FFmpeg. A deterministic WAV image proves routing, 50 ms duration, the exact first selected sample, audible PCM, relative seek, exact frame count, and EOS. Real APE/large-file corpora, appended APEv2 tag metadata, finish-block validation, one-bit DSD block scaling, URL image references, and Windows/macOS build gates remain. | native Rust APL parser + bounded FFmpeg source |
| Partial | HTTP/HLS | Direct HTTP and HTTPS audio streams plus local or remote M3U8/HLS playlists route through Kog's linked libavformat/libavcodec backend; no FFmpeg executable is spawned. Remote URLs are first-class playlist sources, URL fragments are removed before transport, stalled reads have a 15-second bound, streamed reconnect is enabled, and nested HLS protocols are restricted to HTTP(S), TCP/TLS, and crypto so a remote manifest cannot redirect to a local file URL. A generated AC-3 stream and an HLS manifest/segment served by an in-process loopback server gate native probing, PCM decode, sample properties, and end-to-end segment fetching. Mutable live-playlist refresh, ICY metadata updates, redirects/authentication/proxies/TLS corpora, network buffering policy, failure recovery during playback, and Windows/macOS runtime gates remain. | linked FFmpeg network protocols; no external binary |

## Tracker and computer-music formats

| State | Cog family | Extensions / behavior | Kog backend |
| --- | --- | --- | --- |
| Partial | OpenMPT | Cog's exact libopenmpt 0.8.7 release is pinned and built from source. The backend verifies and advertises the 68 native extensions returned by that build plus Cog's MDZ, MDR, S3Z, XMZ, ITZ, and MPTMZ compressed-module aliases, expands zero-based subsongs, reads common metadata and the long native format name, renders 44.1 kHz stereo float PCM with Cog's gain, stereo-separation, 8-tap interpolation, volume-ramping, synchronous-seek, normal-repeat, and Amiga-emulation defaults, and seeks by time. The aliases are content-detected and safely extracted by Kog's bounded archive layer before libopenmpt receives the contained module, retaining the outer archive identity. A generated ProTracker MOD passes format routing, metadata, duration, audible PCM, and seek tests; ZIP-wrapped copies gate all six aliases through routing, extraction, metadata, audible PCM, and seek. The full 68-format corpus, alternate alias archive encodings, repeat-one, and configurable sample rate/interpolation remain. | `libopenmpt` 0.8.7 at `11363ff11ba021b1cf1533da17d9fdf20c8d883c` + bounded libarchive extraction |
| Partial | DUMB/BASSMODS/modplay/playptmod | Native IT, XM, S3M, MOD, STM, PTM, MTM, 669, PSM, J2B, DSM, AMF, OKT, UMX, MO3, STK, M15, and FST routing now overlaps the OpenMPT backend. Cog's six compressed aliases are routed and decoded; any remaining behavioral differences require corpus-driven compatibility fallbacks. | libopenmpt first; compatibility fallbacks only where corpus proves a difference |
| Partial | HivelyTracker | AHX and HVL route through the official HivelyTracker 1.9 replayer at `f393ca7`. Kog reads the native title, expands the main song and declared subsongs, scans two loops with a two-hour safety bound, applies Cog's default eight-second fade, renders stereo float PCM at 44.1 kHz, and supports restart-and-skip seeking. Official upstream AHX/HVL songs and a deterministic two-subsong HVL derivative pass routing, duration, audible PCM, and seek tests. A wider corpus, repeat-one, configurable rate/fade, and comparison with Cog's modified blip-buffer output remain. | `hivelytracker` 1.9 plus upstream fixes at `f393ca7c6416f00bcb574b334a7e8b57dcb19eb2` |
| Partial | Organya | Org-02 and Org-03 route through `orgorg` 0.2.1, with 44.1 kHz stereo float PCM, Cog's default two-loop/eight-second-fade timing, loop metadata, and restart-and-skip seek. Kog accepts a user-supplied `soundbank.wdb` or extracted `wavetable.dat`/`drums.dat` pair and deliberately does not redistribute Cave Story's synthesis assets. A deterministic Org-02 song and synthetic bank pass routing, duration, audible PCM, seek, and end-of-stream tests. An independently redistributable wider song corpus, percussion comparison against Cog, Cog's Lanczos behavior, repeat-one, configurable sample rate/loop/fade, and a soundbank picker remain. | `orgorg` 0.2.1 |
| Partial | Syntrax | JXS routes through canonical `syntrax-c` at `1184fb9`, whose source is byte-for-byte identical to the C renderer in the Cog reference. Kog translates none of Cog's Objective-C plugin: a separate helper validates the packed header, object counts, structural ranges, synthesis indices, sample bounds, and 256 MiB file cap before invoking the portable library. It expands zero-based subsongs, reports trimmed native titles, renders Cog's cubic 44.1 kHz signed-16 stereo path with two-loop/eight-second-fade timing, and seeks by deterministic restart/discard. A generated two-subsong JXS fixture gates routing, title/track metadata, audible PCM, seek, fade/EOS, invalid counts, missing subsongs, and archive-contained subsong expansion. A broad independently redistributable corpus, configurable sample rate/interpolation/loop/fade, Windows/macOS runtime gates, and direct Cog PCM comparison remain. | separate `syntrax-c` `1184fb9ef562d20dab26e419052982d1c3329b76` GPL-3.0-only helper; no Objective-C translation |

## Chiptune and game audio

| State | Cog family | Extensions / behavior | Kog backend |
| --- | --- | --- | --- |
| Partial | Game Music Emu | A real PCM backend is wired for AY, GBS, HES, KSS, NSF/NSFE, SAP, and SPC, with metadata, seek, M3U companions, multitrack expansion where applicable, and Cog-compatible default loop/fade timing. Cog's fork-specific SFM decoder is separately routed through the exact portable GME/higan core in a bundled GPL-2.0-only helper, with bounded input/state metadata, native tags, 32 kHz stereo PCM, loop/fade timing, and restart-and-render seek. The official NSF fixture and a fully generated SFM state pass probe, non-silent decode, seek, metadata, duration/fade, and exact end-of-stream gates; the other routed formats still need corpus validation. SGC remains unrouted because neither upstream libGME 0.6.5 nor Cog's pinned GME tree implements the advertised type. Configurable synthesis policy also remains. | `game-music-emu` (upstream libGME 0.6.5) + separate Cog `Spc_Sfm`/higan GPL-2.0-only helper |
| Partial | HighlyComplete NCSF | NCSF and minincsf route through the official SSEQPlayer at `77222d3` and psflib at `95509e0`. The backend resolves relative `_lib` chains, selects the reserved-field SSEQ, reads title/artist/game/genre/date tags, implements tagged length/fade plus Cog's 150-second/eight-second defaults, renders 44.1 kHz stereo PCM with sinc interpolation, and seeks by deterministic reconstruction and exact frame discard. A generated SDAT/PSF pair gates routing, metadata, audible PCM, seek, fade/EOS, default timing, malformed structures, missing libraries, mini-library tag precedence, and an archived mini/library pair without game data. A broad redistributable NCSF corpus, configurable fade/rate, and comparison against Cog remain. | SSEQPlayer `77222d3657adff358fb4e610d3e56bb7ada8ec24` + psflib `95509e0c6f13d769593bbf51a1b0e0efdc355ba1` + zlib |
| Partial | HighlyComplete GSF | GSF and minigsf route through Cog's exact mGBA revision at `f6b1854` and psflib at `95509e0`. The backend bounds merged ROM data to 32 MiB, resolves relative `_lib` chains, reads title/artist/game/genre/date tags, implements tagged length/fade plus Cog's 150-second/eight-second defaults, renders mGBA's 32,768 Hz stereo PCM, and seeks by deterministic reconstruction and exact frame discard. A generated PSF containing an original ARM program that drives the GBA PSG gates routing, metadata, audible PCM, seek, fade/EOS, default timing, malformed programs, missing libraries, mini-library tag precedence, and an archived mini/library pair. Kog uses mGBA's HLE startup and includes no Nintendo BIOS, logo, or game data. A broad redistributable GSF corpus, configurable fade/rate, and comparison against Cog remain. | mGBA `f6b1854c373fd7cdf8571b9d8568f68bc2decdb1` + psflib `95509e0c6f13d769593bbf51a1b0e0efdc355ba1` + zlib |
| Partial | HighlyComplete QSF | QSF and miniqsf route through the standalone Highly Quixotic core at `1150a17` and psflib at `95509e0`; Kog uses the cross-platform library Cog wraps instead of translating its Objective-C plugin. The backend bounds merged KEY, Z80, and SMP sections, resolves relative `_lib` chains, reads title/artist/game/genre/date tags, implements tagged length/fade plus Cog's 150-second/eight-second defaults, renders the native 24,038 Hz stereo PCM, and seeks by deterministic reconstruction and exact frame discard. A generated PSF containing an original Z80 program and synthetic QSound waveform gates routing, metadata, audible PCM, seek, fade/EOS, default timing, malformed sections, missing libraries, mini-library tag precedence, and an archived mini/library pair. A broad redistributable QSF corpus, configurable fade/rate, and comparison against Cog remain. | Highly Quixotic `1150a17696dbd044f215f823166a5f2b6519cd5f` + psflib `95509e0c6f13d769593bbf51a1b0e0efdc355ba1` + zlib |
| Partial | HighlyComplete SSF/DSF | SSF, minissf, DSF, and minidsf route through the standalone Highly Theoretical core at `2998a4b` and psflib at `95509e0`; Kog uses the cross-platform emulator beneath Cog instead of translating its Objective-C plugin. The bridge bounds merged Saturn programs to 512 KiB and Dreamcast programs to 8 MiB, resolves relative `_lib` chains, reads title/artist/game/genre/date tags, implements tagged length/fade plus Cog's 150-second/eight-second defaults, renders native 44.1 kHz stereo PCM, and seeks by deterministic reconstruction and exact frame discard. Kog builds the GPL-2.0-or-later C68k route and excludes the separately licensed Musashi and Starscream alternatives. Generated PSFs with original 68000 and ARM programs gate both consoles, metadata, audible PCM after seek, fade/EOS, default timing, malformed programs, missing libraries, mini-library tag precedence, and an archived SSF mini/library pair. A broad redistributable SSF/DSF corpus, configurable fade/rate, and comparison against Cog remain. | Highly Theoretical `2998a4bf550949cd2daee249e725a64462cf15e0` + C68k + psflib `95509e0c6f13d769593bbf51a1b0e0efdc355ba1` + zlib |
| Partial | HighlyComplete USF | USF and miniusf route through the maintained LazyUSF2 core at `f771b33` and psflib at `95509e0`; Kog uses the cross-platform N64 emulator beneath Cog instead of translating its Objective-C wrapper. The bridge rejects executable PSF payloads, bounds reserved ROM writes to 64 MiB and save-state writes to `0x80275c`, resolves relative `_lib` chains, preserves outer-file common tags plus nested `_enablecompare`/`_enablefifofull`, implements tagged length/fade plus Cog's 150-second/eight-second defaults, renders resampled 44.1 kHz stereo PCM, and seeks through LazyUSF2's restart/discard API. The build uses the upstream x86/x86-64 dynarec where supported and a cached-interpreter fallback on other targets. A sparse generated Project64 state with original MIPS sound code gates routing, metadata, audible PCM after seek, fade/EOS, defaults, malformed and missing dependencies, mini-library precedence, and an archived miniUSF pair without Nintendo firmware, ROMs, or game code. Cog's ten-second leading-silence removal, a broad redistributable corpus, configurable timing, and direct comparison remain. | LazyUSF2 `f771b33f3a9f96f351ab43635a4b8529fa26a47d` + psflib `95509e0c6f13d769593bbf51a1b0e0efdc355ba1` + zlib |
| Partial | HighlyComplete PSF | PSF and miniPSF route through libupse at `e3f1192`; Kog uses the existing cross-platform HLE emulator instead of translating Cog's Objective-C plugin or copying its embedded BIOS. GPL-2.0-only libupse is compiled into a separate `kog-psf-helper`, not linked into Kog. The helper validates PSF/dependency sizes, section arithmetic, CRC/decompression, 2 MiB executable RAM bounds, fixed tag limits, missing libraries, and sixteen-level nesting before the legacy core runs; it rejects blank tag lines and `_lib9` because both reach unsafe paths in this libupse revision. A versioned binary stream returns metadata and 44.1 kHz stereo PCM; Rust applies common tags, exact fractional timing, Cog's 150-second/eight-second defaults, one fade, exact EOS, and restart/discard seeking. Generated PSF/miniPSF fixtures with original MIPS/SPU code gate routing, metadata, audible PCM, seek, fade/EOS, defaults, malformed input rejection, missing libraries, and an archived pair. A broad redistributable corpus, Cog's leading-silence scan, configurable timing, safe `_lib9` support, Windows/macOS runtime gates, and direct comparison remain. | separate libupse `e3f1192e55e3eb5e1a22b84ed2c4f5a0e0786d85` GPL-2.0-only helper + zlib; no libupse linkage in Kog |
| Partial | HighlyComplete PSF2 | PSF2 and miniPSF2 route through Play! at `04bde0d`; Kog reuses its portable PSF filesystem, HLE IOP BIOS, MIPS CPU, and SPU2 core instead of translating Cog's Objective-C wrapper or shipping Sony firmware. Play! is compiled into a separate `kog-psf2-helper` and is not linked into Kog. The helper bounds root/dependency files, aggregate filesystem data, compressed blocks, tags, and nesting; rejects missing, absolute, or cyclic libraries; verifies zlib blocks; and validates the root IRX's ELF tables, load range, and IOP module section. The shared protocol returns metadata and 44.1 kHz stereo PCM; Rust applies common tags, exact tagged or 150-second/eight-second default timing, one fade, exact EOS, and restart/discard seeking. Generated PSF2/miniPSF2 fixtures with original MIPS/SPU2 code gate routing, metadata, audible PCM after seek, defaults/EOS, malformed filesystem and IRX input, dependency failures and precedence, and an archived pair. A broad redistributable corpus, Cog's leading-silence scan, configurable timing, Windows/macOS runtime gates, and direct comparison remain. | separate Play! `04bde0df87ee7c0e2f0151b51bb2cc22c88541da` BSD helper + permissive dependencies + system zlib; no Play! linkage in Kog |
| Partial | HighlyComplete 2SF | 2SF and mini2SF route through official melonDS 1.1 at `b86390e`; Kog reuses the maintained portable Nintendo DS CPU, memory, free BIOS/generated firmware, and SPU core instead of translating Cog's Objective-C++ wrapper or copying firmware. The GPL-3.0-or-later core and adapter are compiled into a separate `kog-2sf-helper` for fault isolation. psflib resolves relative dependencies; the adapter bounds root/dependency files, ROM mappings to 512 MiB, save mappings to 128 MiB, SAVE decompression and CRCs, metadata, and Nintendo DS ARM9/ARM7 executable ranges. The shared protocol returns metadata and 32,728 Hz stereo PCM; Rust applies common tags, tagged or 150-second/eight-second default timing, one fade, exact EOS, and restart/discard seeking. Generated full/mini2SF fixtures with original ARM/SPU code gate routing, outer-tag precedence, audible PCM after seek, defaults/EOS, malformed mappings and executable headers, missing/cyclic libraries, and an archived pair. A broad redistributable corpus, Cog's exact fractional-rate and leading-silence behavior, configurable timing, Windows/macOS runtime gates, performance measurement, and direct comparison remain. | separate official melonDS `b86390e4428bf38ce4c1ce0e9ca446d6d25955e8` GPL-3.0-or-later helper + psflib + zlib; no Nintendo assets |
| Partial | HighlyComplete SNSF | SNSF and miniSNSF route through the dedicated libsnsf9x library at `e53bff5`; Kog uses its portable Snes9x-derived player API instead of translating Cog's Objective-C++ wrapper. Because the core carries personal/non-commercial terms incompatible with Kog's GPL-3.0-or-later application, it and the `LicenseRef-Snes9x` adapter are compiled only into the separately identified optional `kog-snsf-helper`. psflib validates CRC/decompression and bounded nesting while the adapter restricts companions to the root directory tree, caps 128 unique files/256 MiB, ROM mappings to the core's 8 MiB limit, SRAM to 128 KiB, and metadata, then passes a sanitized dependency-free SNSF image to libsnsf9x's public C ABI. The shared protocol returns 32 kHz stereo PCM; Rust applies common tags, tagged or 150-second/eight-second default timing, one fade, exact EOS, and restart/discard seeking. Generated full/miniSNSF fixtures with original 65C816/SPC700 code and synthetic BRR data gate routing, outer-tag precedence, audible PCM after seek, defaults/EOS, malformed ROM/SRAM mappings, missing/cyclic/traversing libraries, and an archived pair. A broad independently redistributable corpus, leading-silence behavior, configurable timing, newer-core evaluation, Windows/macOS runtime gates, performance measurement, and direct comparison remain. | optional separate libsnsf9x `e53bff56fbb7c29d5222c60b81a54b762ad9cec7` Snes9x non-commercial helper + LGPL-2.1 APU + MIT psflib + zlib; no libsnsf9x linkage in Kog |
| Partial | libvgm | VGM/VGZ, S98, DRO, and GYM route to Cog's exact pinned libvgm revision with real 24-bit PCM converted to the shared float stream, common tags, native format/version, seek, and Cog-compatible loop/fade/end-silence policy. A deterministic SN76489 VGM 1.50 fixture passes duration, codec, non-silent decode, seek, and live Qt playback. All configured chip cores except YMF278B remain enabled. The bundled YMF278B implementation is GPL-2.0-only and is deliberately excluded from Kog's GPLv3 executable; OPL4 logs require a compatible replacement or separately licensed helper. VGZ, S98, DRO, GYM, loop/fade completion, metadata-bearing files, OPL4 playback, and libvgm subsongs still need corpus gates. | `libvgm` at `867223e7c33d63de115d1ab955f784c44f19040a` |
| Partial | vgmstream | Upstream r2117 is pinned at `05dbda9b` and registered last using its runtime-enumerated 700-plus specialist extensions, while its common extension table remains unclaimed. The backend uses the public API for filesystem and companion-file reopening, TXTP, zero-based subsong expansion, Cog's default two-loop/eight-second-fade timing, up-to-six-channel float PCM, native sample-frame seek, codec/bitrate properties, and sibling `!tags.m3u` title, artist, album, date, and track fields. A generated PlayStation VAG passes routing, metadata, duration, audible PCM, seek, and exact end-of-stream tests. The portable baseline currently enables native codecs and built-in G.722.1; FFmpeg, Vorbis, mpg123, G.719, ATRAC9, CELT, and Speex-backed streams plus a broad real-format corpus, repeat-one, configurable loop/fade, and more companion/subsong fixtures remain. | `vgmstream` r2117 at `05dbda9b930b8d174f03387fb626d97d827d0647` |
| Partial | SID | Cog's exact libsidplayfp `519d1201` revision and reSIDfp core are pinned and built from source. ROM-free PSID files route through a real 44.1 kHz signed-16-to-float source with zero-based subtune expansion, title/artist/release metadata, mono or multi-SID stereo output, Cog's 150-second default plus eight-second fade, stereo-width transform, and 32x accelerated seek with an exact residual. A generated two-subtune PSID passes routing, metadata, audible PCM, seek, ROM-policy, and exact end-of-stream tests. Kog does not redistribute C64 ROMs: RSID and BASIC-compatible tunes fail explicitly until user ROM selection exists. Raw MUS/MUS+STR routing, the song-length database, repeat-one, configurable synthesis rate/filter/fade, user-ROM configuration, and a broad PSID corpus remain. | libsidplayfp 2.4.0a at `519d1201efcc6c97f7cc3506947875d21a9bd195` + reSIDfp |
| Partial | AdPlug | Cog's exact AdPlug `4e0141ab` and libbinio `e2f8d50c` revisions are pinned and built from source. The backend advertises the 51 unique extensions enumerated by that build, expands zero-based subsongs, reads native type/title/author metadata, calculates duration, renders 44.1 kHz stereo through its bundled Nuked OPL3 core, and performs exact restart-and-render seeking. Registry order preserves the MIDI, libvgm, and OpenMPT routes for shared MID, VGM/VGZ/DRO, and S3M extensions. Upstream's `2.CMF` fixture passes routing, duration, audible PCM, seek, and exact end-of-stream tests. Cog's AdPlug database, its behavioral effects, content probing for ambiguous extensions such as MUS, configurable synthesis rate, repeat-one, and a broad 51-format corpus remain. | AdPlug 2.3.4-beta at `4e0141ab41ac4ebf388b765d669eb656376d04fd` + libbinio at `e2f8d50c53102c618d675c3310e09a0e0bdf49cd` + bundled Nuked OPL3 |

## MIDI

| State | Cog family | Extensions / behavior | Kog backend |
| --- | --- | --- | --- |
| Partial | MIDI containers | MID, MIDI, and KAR Standard MIDI Files (formats 0 and 1) plus RIFF RMID are parsed and rendered. They can select RustySynth SF2, OPL3Windows, the optional Nuked SC-55 helper, or in-process Munt MT-32/CM-32L emulation. HMI, HMP/HMQ, DMX MUS, and Miles XMI route through pinned libADLMIDI and Nuked OPL3 with native subsongs, title, duration, seek, and bounded EOS. `.mus` is content-sniffed so non-DMX tracker modules retain the libopenmpt route. An original generated DMX MUS gates routing, audible PCM, seek, malformed input, and EOS; broad independently redistributable HMI/HMP/XMI corpora remain. SMF format 2, MIDS, MDS, LDS, XMF, and MXMF remain missing. | `midi-rustysynth-sf2`, `midi-opl3windows`, `midi-nuked-sc55`, `midi-munt-mt32`, libADLMIDI `d114c31` |
| Partial | SoundFont synthesis | Configurable and persisted SF2 selection, 48 kHz stereo rendering, duration, seek, and end-of-stream behavior are implemented. SF3, per-file flavor selection, and synthesis/effect controls remain missing. | RustySynth |
| Partial | OPL synthesis | Cog's OPL3Windows General MIDI timbre table, 18-voice engine, and Nuked OPL3 1.7.1 core render format-0/1 SMF and RMID with tempo/SMPTE timing, controls, pitch bend, drums, duration, and seek. The separate maintained libADLMIDI backend renders HMI/HMP/HMQ/MUS/XMI through its Nuked OPL3 family and a cleared embedded-bank database. User WOPL bank selection and per-format flavor/effect controls remain. | `midi-opl3windows`, libADLMIDI `d114c31` |
| Partial | Roland emulation | Persisted MIDI choices cover two Roland families. Nuked SC-55 0.6.1 (`50dcdde`) remains a separately licensed optional helper: it detects complete user ROM sets by hash, applies GS startup, streams native-rate stereo PCM, and seeks by restart/suppression. Munt 2.8.2 (`3b05ec2`) is statically linked in-process with no external executable: Kog scans a user-selected directory for a compatible MT-32/CM-32L control/PCM pair using libmt32emu's own identification, applies a valid MT-32 reset, schedules channel and fragmented SysEx streams at exact tempo/SMPTE-derived frames, reports the detected model, renders resampled 48 kHz stereo float PCM, and seeks by deterministic reconstruction. Kog includes no Roland ROMs. Protocol/timeline, routing, and missing-ROM gates pass, but real user-ROM audible-PCM/seek corpus gates are still required before fidelity is proven for either family. | Munt/libmt32emu 2.8.2 at `3b05ec276f9e605af86b0eaef7f5eda43477a31f` in-process; optional Nuked SC-55 0.6.1 `50dcddeacfdf6fcfbcc948ca08cf4ad5fac08980` helper; user-supplied ROMs; no Objective-C translation |

## Containers, playlists, and metadata

| State | Cog family | Extensions / behavior | Kog backend |
| --- | --- | --- | --- |
| Partial | CueSheet | External CUE files expand their AUDIO tracks, resolve relative Windows or POSIX paths, preserve Cog's persistent artist/title/genre/date/ReplayGain scanner state, support multiple FILE blocks, and interpret INDEX 01 as either 75 Hz CD frames or raw samples. UTF-8, BOM-marked UTF-16, and Windows-1252 fallback are implemented. OGG/Opus/FLAC/WV/MP3 are content-probed and only preempt their normal backend when FFmpeg exposes an embedded CUESHEET tag. Track metadata overrides underlying metadata and the shared bounded FFmpeg source enforces relative seek and exact EOS. Generated WAV/CUE and tagged-MP3 fixtures pass expansion, decoder-priority, multi-file, metadata, duration, audible PCM, exact boundary, seek, and EOS gates. URL sources, broader charset detection, one-bit DSD scaling, ReplayGain application, stable track-number fragments, gapless same-image transitions, real-world corpora, and Windows/macOS gates remain. | native Rust CUE parser + FFmpeg metadata probe/delegated bounded source |
| Partial | Playlists | M3U/M3U8 preserve line order, ignore comments and EXTINF metadata like Cog, normalize classic-Mac/Windows line endings, and route HLS tags to FFmpeg rather than misreading segments as songs. PLS preserves line order and consumes only case-insensitive `File…=` keys. Both use Cog's UTF-8, GB18030, Windows-1251, then Latin-1 decoding order; resolve relative POSIX/Windows paths, local `file://` URLs, and remote HTTP(S) entries; retain trailing numeric fragments; and expand nested local playlists with a 32-level limit and cycle detection. Numeric fragments use Cog's zero-based subsong convention, while CUE fragments resolve by declared track number. Cog-style M3U/M3U8 and PLS writing uses safe relative local paths, file URLs where ambiguity requires them, remote URLs, numeric fragments, and `unpack://fex` URLs that reopen one selected archived member without serializing temporary extraction paths. The hamburger and playlist context menu expose Save As and Save Selection As through a platform-native save dialog. Missing, unsupported, and malformed entries produce explicit warnings without discarding valid tracks. Deterministic fixtures pass encoding, comment/metadata ignoring, order, path normalization, file-URL, nested M3U-to-PLS, CUE-fragment, missing-entry, unsupported-entry, remote-entry preservation, HLS routing, cycle, exact writer text, all-source-identity round trips, and selected archived-subsong reopen gates. Remote conventional-playlist recursion, non-numeric/TXTP fragments, automatic session persistence, broad corpora, and Windows/macOS runtime gates remain. | native Rust playlist reader/writer + remote/archive source model + linked FFmpeg |
| Partial | Archives | ZIP, RAR, 7Z, RSN, VGM7Z, raw GZ, and Cog's MDZ/MDR/S3Z/XMZ/ITZ/MPTMZ compressed-module aliases are expanded in physical archive order before decoder selection through `compress-tools` and the system libarchive. Kog keeps a safe temporary companion tree alive for path-based decoders, assigns stable `archive :: entry` identities independent of temporary paths, and rejects traversal, duplicate destinations, non-regular entries, more than 16,384 entries, files over 4 GiB, or archives over 8 GiB expanded. Filename decoding follows Cog's UTF-8, GB18030, Windows-1251, then Latin-1 fallback. Deterministic ZIP/GZ WAV fixtures pass expansion and playback; real 7Z/RAR5 fixtures pass extraction under their Cog aliases; ZIP-wrapped MOD copies gate every compressed-module alias through metadata, audible PCM, and seek; a ZIP-contained APL/WAV pair proves relative image resolution; and archived NCSF, GSF, QSF, SSF, USF, PSF, PSF2, SNSF, and 2SF mini/library pairs prove dependency resolution. Passwords, multipart and unusual solid archives, nested archives, archive-contained playlist/CUE corpus coverage, broad filename/format corpora, configurable limits, caching, and Windows/macOS runtime gates remain. | native Rust policy + `compress-tools` 0.16.1 + system libarchive |
| Partial | Tags | Cog's TagLib metadata read/write surface and album art. Common read-only fields are wired through Lofty; editing and artwork are missing. | Lofty plus TagLib fallback |

## Cross-cutting Cog behavior still required

- decoder priority and content probing rather than extension-only selection;
- remote and reusable local source abstractions; HTTP(S), HLS, archive identity,
  and companion-backed local extraction are present, while reusable
  random-access network caching and extraction caching remain;
- stable fragment identifiers and persisted subsong identity across every
  remaining multitrack backend;
- configurable loop counts, fade length, and indefinite playback;
- ReplayGain, gapless playback, general resampling, direct Cog equalizer PCM
  comparison, pitch/tempo, automatic audio-device hot-plug failover, and
  output-buffer controls;
- metadata editing, album art, ratings, play counts, and library
  persistence;
- Last.fm, notifications, remote control, spectrum/visualization, lyrics, and
  media-key integration.
